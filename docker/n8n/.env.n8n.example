# Example n8n Configuration for Tailscale
# 
# Copy these lines to your main .env file and update with your Tailscale IP
# Example Tailscale IP: 100.75.7.93 (replace with your actual IP)

# ============================================================================
# n8n Network Configuration (for Tailscale)
# ============================================================================

# Your Tailscale IP address (check with: tailscale ip -4)
N8N_HOST=100.75.7.93

# Protocol (http or https)
N8N_PROTOCOL=http

# Editor URL - this is where the n8n UI is accessible
# Use the proxy port (8081) for frontend access
N8N_EDITOR_BASE_URL=http://100.75.7.93:8081

# Webhook URL - used for webhook integrations like Telegram bots
# Must be externally accessible
N8N_WEBHOOK_URL=http://100.75.7.93:8081/

# ============================================================================
# Port Configuration (optional - defaults shown)
# ============================================================================

# Backend port (internal API)
PORT_N8N=5678

# Proxy port (frontend access - recommended)
PORT_N8N_PROXY=8081

# ============================================================================
# Instructions
# ============================================================================
#
# 1. Find your Tailscale IP:
#    tailscale ip -4
#
# 2. Update N8N_HOST, N8N_EDITOR_BASE_URL, and N8N_WEBHOOK_URL with your IP
#
# 3. Add these lines to your main .env file (or create one from .env.example)
#
# 4. Start services:
#    docker compose up -d n8n n8n-proxy
#
# 5. Access n8n at:
#    http://YOUR-TAILSCALE-IP:8081
#
